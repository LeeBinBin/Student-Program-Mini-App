<view class="container">
  <!-- 顶部导航 -->
  <view class="top-nav">
    <view class="nav-back" bindtap="navigateBack">
      <text class="back-icon">←</text>
    </view>
    <view class="nav-title">课程管理</view>
    <view class="nav-actions">
      <view class="action-item" bindtap="addCourse">
        <text class="action-icon">+</text>
      </view>
    </view>
  </view>

  <!-- 搜索和筛选 -->
  <view class="search-filter">
    <view class="search-box">
      <text class="search-icon">🔍</text>
      <input class="search-input" placeholder="搜索课程名称、学科或年级" bindinput="onSearch" value="{{searchKeyword}}" />
    </view>
    <view class="filter-section">
      <view class="filter-item" bindtap="showFilter">
        <text>{{selectedSubject || '学科'}}</text>
        <text class="filter-arrow">▼</text>
      </view>
      <view class="filter-item" bindtap="showGradeFilter">
        <text>{{selectedGrade || '年级'}}</text>
        <text class="filter-arrow">▼</text>
      </view>
      <view class="filter-item" bindtap="showTypeFilter">
        <text>{{selectedType || '课程类型'}}</text>
        <text class="filter-arrow">▼</text>
      </view>
    </view>
  </view>

  <!-- 课程列表 -->
  <view class="courses-list">
    <view wx:for="{{courses}}" wx:key="id" class="course-item">
      <view class="course-header">
        <view class="course-info">
          <view class="course-name">{{item.name}}</view>
          <view class="course-details">
            <text class="detail-item">{{item.subject}}</text>
            <text class="detail-divider">|</text>
            <text class="detail-item">{{item.grade}}年级</text>
            <text class="detail-divider">|</text>
            <text class="detail-item">{{item.type}}</text>
          </view>
        </view>
        <view class="course-status" style="background-color: {{item.status === 'active' ? '#D9F99D' : '#FEE2E2'}}">
          {{item.status === 'active' ? '进行中' : '已结束'}}
        </view>
      </view>
      <view class="course-content">
        <view class="course-description">{{item.description}}</view>
        <view class="course-meta">
          <view class="meta-item">
            <text class="meta-icon">👥</text>
            <text class="meta-text">{{item.studentCount}}名学生</text>
          </view>
          <view class="meta-item">
            <text class="meta-icon">📅</text>
            <text class="meta-text">{{item.startDate}} 至 {{item.endDate}}</text>
          </view>
        </view>
      </view>
      <view class="course-actions">
        <view class="action-btn primary-btn" bindtap="viewCourseDetail" data-id="{{item.id}}">
          <text class="action-text">查看详情</text>
        </view>
        <view class="action-btn secondary-btn" bindtap="editCourse" data-id="{{item.id}}">
          <text class="action-text">编辑</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 无数据提示 -->
  <view wx:if="{{courses.length === 0}}" class="empty-state">
    <text class="empty-icon">📚</text>
    <view class="empty-text">暂无课程数据</view>
    <view class="empty-desc">点击右上角添加课程</view>
  </view>

  <!-- 学科筛选弹窗 -->
  <view wx:if="{{showSubjectFilter}}" class="modal" catchtap="hideSubjectFilter">
    <view class="modal-content" catchtap="catchTap">
      <view class="modal-header">
        <view class="modal-title">选择学科</view>
        <view class="modal-close" bindtap="hideSubjectFilter">×</view>
      </view>
      <view class="modal-body">
        <view class="filter-option" bindtap="selectSubject" data-value="">
          <text class="option-text">全部</text>
          <text wx:if="{{!selectedSubject}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectSubject" data-value="数学">
          <text class="option-text">数学</text>
          <text wx:if="{{selectedSubject === '数学'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectSubject" data-value="英语">
          <text class="option-text">英语</text>
          <text wx:if="{{selectedSubject === '英语'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectSubject" data-value="语文">
          <text class="option-text">语文</text>
          <text wx:if="{{selectedSubject === '语文'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectSubject" data-value="科学">
          <text class="option-text">科学</text>
          <text wx:if="{{selectedSubject === '科学'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectSubject" data-value="其他">
          <text class="option-text">其他</text>
          <text wx:if="{{selectedSubject === '其他'}}" class="option-check">✓</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 年级筛选弹窗 -->
  <view wx:if="{{showGradeFilter}}" class="modal" catchtap="hideGradeFilter">
    <view class="modal-content" catchtap="catchTap">
      <view class="modal-header">
        <view class="modal-title">选择年级</view>
        <view class="modal-close" bindtap="hideGradeFilter">×</view>
      </view>
      <view class="modal-body">
        <view class="filter-option" bindtap="selectGrade" data-value="">
          <text class="option-text">全部</text>
          <text wx:if="{{!selectedGrade}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectGrade" data-value="1">
          <text class="option-text">1年级</text>
          <text wx:if="{{selectedGrade === '1'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectGrade" data-value="2">
          <text class="option-text">2年级</text>
          <text wx:if="{{selectedGrade === '2'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectGrade" data-value="3">
          <text class="option-text">3年级</text>
          <text wx:if="{{selectedGrade === '3'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectGrade" data-value="4">
          <text class="option-text">4年级</text>
          <text wx:if="{{selectedGrade === '4'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectGrade" data-value="5">
          <text class="option-text">5年级</text>
          <text wx:if="{{selectedGrade === '5'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectGrade" data-value="6">
          <text class="option-text">6年级</text>
          <text wx:if="{{selectedGrade === '6'}}" class="option-check">✓</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 课程类型筛选弹窗 -->
  <view wx:if="{{showTypeFilter}}" class="modal" catchtap="hideTypeFilter">
    <view class="modal-content" catchtap="catchTap">
      <view class="modal-header">
        <view class="modal-title">选择课程类型</view>
        <view class="modal-close" bindtap="hideTypeFilter">×</view>
      </view>
      <view class="modal-body">
        <view class="filter-option" bindtap="selectType" data-value="">
          <text class="option-text">全部</text>
          <text wx:if="{{!selectedType}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectType" data-value="常规课程">
          <text class="option-text">常规课程</text>
          <text wx:if="{{selectedType === '常规课程'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectType" data-value="跨境适配">
          <text class="option-text">跨境适配</text>
          <text wx:if="{{selectedType === '跨境适配'}}" class="option-check">✓</text>
        </view>
        <view class="filter-option" bindtap="selectType" data-value="拓展课程">
          <text class="option-text">拓展课程</text>
          <text wx:if="{{selectedType === '拓展课程'}}" class="option-check">✓</text>
        </view>
      </view>
    </view>
  </view>
</view>
